# Plan — Task 0036

1. Remove stale automatic save-prompt wording from `docs/TECHNICAL_EXPLANATION.md`.
2. Remove `.cursor` references from `docs/MCP.md` while preserving generic MCP setup guidance.
3. Mark tasks 0027 and 0035 as `done` in their `TASK.md` metadata.
4. Update `INDEX.md`, `activeContext.md`, and `progress.md` to reflect final status.
5. Run documentation-separation grep and `python -m retail_agent.evals`.
6. Commit the docs/memory-bank changes with a Conventional Commit message.
